# Flash Sale Project Handoff

Last updated: 2026-07-02 03PM

## Canonical Project Folder

```text
D:\LearnCSharp14DotNet10\FlashSale
```

## What Was Completed Since Last Handoff

### Repository hygiene fixed

`.gitignore` was expanded so build/test outputs no longer pollute `git status`.

Important ignored outputs now include:

```text
**/bin/
**/obj/
*.log
TestResults/
coverage outputs
.vs/
local user files
```

Tracked build artifacts and runtime logs were removed from git in commit:

```text
aa3a21f Ignore build artifacts and expand integration tests
```

After compile/test, `bin` and `obj` no longer appear in git status.

### Integration test coverage expanded

Integration tests were added for:

- Product CRUD happy path
- Campaign CRUD happy path
- FlashSaleItem validation failures
- FlashSaleItem duplicate product conflict
- Order creation happy path
- Order oversell rejection
- Order missing customer name
- Order empty items
- Order campaign outside time window

Current integration test support uses:

```text
tests\FlashSale.IntegrationTests\Api\FlashSaleApiFactory.cs
```

The factory uses EF Core InMemory and ignores:

```text
InMemoryEventId.TransactionIgnoredWarning
```

This is needed because `POST /api/orders` uses transactions.

### Orders domain model added

New domain files:

```text
src\FlashSale.Domain\Entities\Order.cs
src\FlashSale.Domain\Entities\OrderItem.cs
src\FlashSale.Domain\Entities\OrderStatus.cs
```

Current relationship:

```text
Order 1 - N OrderItem
OrderItem N - 1 FlashSaleItem
FlashSaleItem N - 1 Product
FlashSaleItem N - 1 FlashSaleCampaign
```

`FlashSaleItem` now has:

```csharp
public List<OrderItem> OrderItems { get; set; } = [];
```

### Orders persistence added

New EF configuration files:

```text
src\FlashSale.Infrastructure\Persistence\Configurations\OrderConfiguration.cs
src\FlashSale.Infrastructure\Persistence\Configurations\OrderItemConfiguration.cs
```

`FlashSaleDbContext` now includes:

```csharp
public DbSet<Order> Orders { get; set; } = null!;
public DbSet<OrderItem> OrderItems { get; set; } = null!;
```

Migration added:

```text
src\FlashSale.Infrastructure\Persistence\Migrations\20260701071922_AddOrders.cs
```

Database migration status:

```text
20260630071414_InitialCreate
20260630075642_AddFlashSaleCampaigns
20260630092530_AddFlashSaleItems
20260701071922_AddOrders
```

### Orders API started

New API file:

```text
src\FlashSale.Api\Orders\OrderEndpoints.cs
```

`Program.cs` now maps:

```csharp
app.MapOrderEnpoints();
```

Current endpoint:

```text
POST /api/orders
```

Current create order flow:

1. Validate customer name and email.
2. Validate order items.
3. Load flash sale items with campaign and product.
4. Check flash sale item exists.
5. Check item is active.
6. Check campaign is active.
7. Check campaign is currently running.
8. Check stock.
9. Create order and order items.
10. Snapshot unit price from flash sale item sale price.
11. Calculate line totals and order total.
12. Increase sold quantity.
13. Save changes inside a transaction.

Important current polish item:

```text
OrderEndPoints should be renamed to OrderEndpoints
MapOrderEnpoints should be renamed to MapOrderEndpoints
```

The typo works because `Program.cs` calls the same typo, but it should be cleaned before pushing.

### Rules file created and expanded

Rules folder created:

```text
rules
```

Current rules file:

```text
rules\rules-020726-1401.md
```

It now documents:

- Repository hygiene rules
- Migration rules
- Product rules
- Campaign rules
- FlashSaleItem rules
- Integration testing rules
- Orders domain rules
- `POST /api/orders` flow
- Expected failures
- Handoff rules

Future work should follow this rules file unless a newer rules file supersedes it.

## Latest Test Status

Latest command:

```powershell
dotnet test
```

Latest result:

```text
Passed: 28 unit tests
Passed: 12 integration tests
```

## Current Git Status Summary

There are many legitimate uncommitted source changes for the Orders feature and rules/handoff work.

Expected changed or new paths include:

```text
rules\rules-020726-1401.md
handoffs\handoff-02072026-03PM.md
src\FlashSale.Api\Program.cs
src\FlashSale.Api\Orders\OrderEndpoints.cs
src\FlashSale.Domain\Entities\Order.cs
src\FlashSale.Domain\Entities\OrderItem.cs
src\FlashSale.Domain\Entities\OrderStatus.cs
src\FlashSale.Domain\Entities\FlashSaleItem.cs
src\FlashSale.Infrastructure\Persistence\FlashSaleDbContext.cs
src\FlashSale.Infrastructure\Persistence\Configurations\OrderConfiguration.cs
src\FlashSale.Infrastructure\Persistence\Configurations\OrderItemConfiguration.cs
src\FlashSale.Infrastructure\Persistence\Migrations\20260701071922_AddOrders.cs
src\FlashSale.Infrastructure\Persistence\Migrations\20260701071922_AddOrders.Designer.cs
src\FlashSale.Infrastructure\Persistence\Migrations\FlashSaleDbContextModelSnapshot.cs
tests\FlashSale.IntegrationTests\Api\FlashSaleApiFactory.cs
tests\FlashSale.IntegrationTests\Api\OrderApiTests.cs
tests\FlashSale.UnitTests\Orders\OrderCalculationTests.cs
```

No `bin` or `obj` output should be committed.

## Recommended Next Tasks

### Step 1: Polish Orders naming

Rename:

```text
OrderEndPoints -> OrderEndpoints
MapOrderEnpoints -> MapOrderEndpoints
```

Update `Program.cs` accordingly.

### Step 2: Add GET order endpoint

Add:

```text
GET /api/orders/{id}
```

Response should include order header plus order item details:

```text
Order id
CustomerName
CustomerEmail
Status
TotalAmount
Items:
  FlashSaleItemId
  ProductName
  CampaignName
  Quantity
  UnitPrice
  LineTotal
```

### Step 3: Add more order validation tests

Recommended next tests:

- Rejects missing customer email
- Rejects quantity <= 0
- Rejects missing flash sale item id
- Rejects non-existent flash sale item
- Rejects inactive flash sale item
- Rejects inactive campaign

### Step 4: Start concurrency design

Before making this production-grade flash sale checkout, design:

- How to prevent concurrent oversell in PostgreSQL
- Whether to use row version, explicit locking, or conditional update
- Idempotency keys for duplicate purchase requests
- Future reservation TTL behavior
