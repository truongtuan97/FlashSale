# Flash Sale Project Handoff

Last updated: 2026-07-09 03PM Asia/Bangkok

## Canonical Project Folder

```text
D:\LearnCSharp14DotNet10\FlashSale
```

## What Was Completed Since Last Handoff

### PostgreSQL-backed order concurrency tests added

Added a PostgreSQL-specific API test factory:

```text
tests\FlashSale.IntegrationTests\Api\PostgresFlashSaleApiFactory.cs
```

The factory reads the base connection string from:

```text
FLASHSALE_TEST_POSTGRES
```

It creates a unique PostgreSQL database for each factory instance, applies EF Core migrations, keeps the production `EfFlashSaleStockUpdater`, and drops the test database during cleanup.

### Order PostgreSQL concurrency coverage added

Added:

```text
tests\FlashSale.IntegrationTests\Api\OrderPostgresConcurrencyTests.cs
```

Covered scenarios:

1. Two concurrent requests compete for the final unit of stock.
2. Duplicate lines for the same `FlashSaleItemId` are aggregated for stock checks.
3. Multi-item order rolls back stock updates if one item has insufficient stock.
4. Successful multi-item order increments stock for every item.

### PostgreSQL provider added to integration tests

Updated:

```text
tests\FlashSale.IntegrationTests\FlashSale.IntegrationTests.csproj
```

Added:

```xml
<PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="10.0.2" />
```

### Local environment file ignored

Updated:

```text
.gitignore
```

Added `.env-*` so local files such as `.env-test` are not accidentally committed.

### Design note added

Added:

```text
docs\docs-09072026-1458.md
```

The doc records why PostgreSQL-backed tests are separate from the normal EF InMemory integration tests.

## Important Files

```text
.gitignore
docs\docs-09072026-1458.md
handoffs\handoff-09072026-03PM.md
tests\FlashSale.IntegrationTests\FlashSale.IntegrationTests.csproj
tests\FlashSale.IntegrationTests\Api\PostgresFlashSaleApiFactory.cs
tests\FlashSale.IntegrationTests\Api\OrderPostgresConcurrencyTests.cs
```

## Current Test Status

Reported by user after adding the PostgreSQL tests:

```text
PostgreSQL order concurrency tests passed.
```

Recommended verification before commit:

```powershell
dotnet test tests\FlashSale.IntegrationTests\FlashSale.IntegrationTests.csproj --filter FullyQualifiedName~OrderPostgresConcurrencyTests
dotnet test
```

The PostgreSQL-specific test command requires `FLASHSALE_TEST_POSTGRES` to be set in the shell running the tests.

## Migration / Database Status

No new migration was required.

The PostgreSQL test factory applies existing migrations to a temporary test database and drops that database during cleanup.

Current order-related migration remains:

```text
20260701071922_AddOrders
```

## Current Git Status Summary

Expected changed or new paths for this handoff:

```text
.gitignore
docs\docs-09072026-1458.md
handoffs\handoff-09072026-03PM.md
tests\FlashSale.IntegrationTests\FlashSale.IntegrationTests.csproj
tests\FlashSale.IntegrationTests\Api\PostgresFlashSaleApiFactory.cs
tests\FlashSale.IntegrationTests\Api\OrderPostgresConcurrencyTests.cs
```

Local `.env-test` files should remain untracked and should not be committed.

No `bin`, `obj`, log, test output, or local runtime artifacts should be committed.

## Recommended Next Tasks

### Step 1: Re-run full verification

Run the PostgreSQL order concurrency test filter, then run the full test suite:

```powershell
dotnet test tests\FlashSale.IntegrationTests\FlashSale.IntegrationTests.csproj --filter FullyQualifiedName~OrderPostgresConcurrencyTests
dotnet test
```

### Step 2: Review PostgreSQL test code quality

Review formatting and duplication in:

```text
tests\FlashSale.IntegrationTests\Api\OrderPostgresConcurrencyTests.cs
tests\FlashSale.IntegrationTests\Api\PostgresFlashSaleApiFactory.cs
```

Consider extracting shared helpers only if the file grows more.

### Step 3: Design idempotency keys

Create a short design note before implementing duplicate submission protection.

Do not implement payment, checkout finalization, or reservation TTL before idempotency is designed.

### Step 4: Keep reservation TTL separate

Current rule remains: creating an order immediately increases `SoldQuantity`.

