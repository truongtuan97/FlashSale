# Flash Sale Project Handoff

Last updated: 2026-07-01 01PM

## Canonical Project Folder

```text
D:\LearnCSharp14DotNet10\FlashSale
```

## What Was Completed

### Package warning cleanup

The previous EF Core assembly conflict has been fixed.

Changed package versions:

```text
src\FlashSale.Api\FlashSale.Api.csproj
- Microsoft.AspNetCore.OpenApi 10.0.4 -> 10.0.9
- Added direct Microsoft.OpenApi 2.7.5

src\FlashSale.Infrastructure\FlashSale.Infrastructure.csproj
- Added direct Microsoft.EntityFrameworkCore.Relational 10.0.9

src\FlashSale.Worker\FlashSale.Worker.csproj
- Microsoft.Extensions.Hosting 10.0.4 -> 10.0.9
```

Why:

- Align Microsoft package versions with EF Core `10.0.9`.
- Pin `Microsoft.EntityFrameworkCore.Relational` so consumer projects do not resolve the lower `10.0.4` bound from Npgsql.
- Fix the `Microsoft.OpenApi 2.0.0` high severity advisory by pinning patched `2.7.5`.

### Migration/database verification

Existing migration was inspected:

```text
src\FlashSale.Infrastructure\Persistence\Migrations\20260630092530_AddFlashSaleItems.cs
```

It correctly uses:

```text
RenameColumn StartAt -> StartsAt
RenameColumn EndAt -> EndsAt
CreateTable FlashSaleItems
```

So it preserves existing campaign time data.

Database migration status was checked with:

```powershell
dotnet ef migrations list --project src\FlashSale.Infrastructure --startup-project src\FlashSale.Api
```

Applied migrations:

```text
20260630071414_InitialCreate
20260630075642_AddFlashSaleCampaigns
20260630092530_AddFlashSaleItems
```

No database update is needed right now.

### Runtime/API smoke verification

Scalar was tested at:

```text
http://localhost:5216/scalar/v1
```

Result:

```text
HTTP 200
```

Manual HTTP round trip was tested:

1. Create Product
2. Create FlashSaleCampaign
3. Create FlashSaleItem
4. GET FlashSaleItem
5. Cleanup created test records

Observed result:

```text
totalQuantity = 25
soldQuantity = 3
availableQuantity = 22
```

### Integration test foundation added

The old template integration test was removed:

```text
tests\FlashSale.IntegrationTests\UnitTest1.cs
```

New integration test files:

```text
tests\FlashSale.IntegrationTests\Api\FlashSaleApiFactory.cs
tests\FlashSale.IntegrationTests\Api\FlashSaleItemApiTests.cs
```

`FlashSaleApiFactory` uses:

```text
WebApplicationFactory<Program>
EF Core InMemory provider
Testing environment
```

`Program.cs` now ends with:

```csharp
public partial class Program;
```

This allows `WebApplicationFactory<Program>` to host the Minimal API.

Integration coverage now verifies the real HTTP flow:

```text
POST /api/products
POST /api/flash-sale-campaigns
POST /api/flash-sale-items
GET  /api/flash-sale-items/{id}
```

## Latest Test Status

Latest command:

```powershell
dotnet test
```

Latest result:

```text
Passed: 26 unit tests
Passed: 1 integration test
Warnings: none observed in latest successful run
```

## Important Current Git Changes

Expected source changes:

```text
src\FlashSale.Api\FlashSale.Api.csproj
src\FlashSale.Api\Program.cs
src\FlashSale.Infrastructure\FlashSale.Infrastructure.csproj
src\FlashSale.Worker\FlashSale.Worker.csproj
tests\FlashSale.IntegrationTests\FlashSale.IntegrationTests.csproj
tests\FlashSale.IntegrationTests\Api\FlashSaleApiFactory.cs
tests\FlashSale.IntegrationTests\Api\FlashSaleItemApiTests.cs
tests\FlashSale.IntegrationTests\UnitTest1.cs deleted
handoffs\handoff-01072026-01PM.md
```

## Recommended Next Tasks

### Step 1: Add more integration coverage

Good next tests:

- Product CRUD happy path
- Campaign CRUD happy path
- FlashSaleItem validation failures
- Duplicate `(CampaignId, ProductId)` returns conflict
- Missing product/campaign returns bad request

### Step 2: Start Orders design

Before implementing checkout, design:

- Order and OrderItem entities
- Stock update rule for `SoldQuantity`
- Transaction boundary
- Oversell prevention
- Idempotency key shape

Do not rush directly into purchase flow without concurrency design.
